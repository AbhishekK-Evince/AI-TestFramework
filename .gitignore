# .gitignore

# Dependencies
# Ignore the node_modules directory, which contains all of your project's npm packages.
# This directory can be recreated by running `npm install` or `yarn install`.
/node_modules

# Environment Variables
# Ignore .env files. They contain sensitive information like API keys and database credentials.
# A .env.example file should be committed instead to show what variables are needed.
*.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
# Ignore log files. They are specific to your local environment and can become large.
app.log
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Generated & Temporary Files
# Ignore directories and files that are generated during development or by build processes.
/generated
/exports
/uploads
main.js.new
eng.traineddata
/temp-playwright

# Test Reports & Tooling
# Ignore test results, reports, and configuration files from testing and development tools.
/test-results
/playwright-report
/playwright/.cache
.last-run.json
/.qodo

# IDE & System Files
# Ignore common files generated by IDEs and operating systems.
.DS_Store
Thumbs.db

/dist 
/dist-new 

